<section class="hero is-medium page-hero">
  <div class="hero-body has-text-centered page-hero-content">
    <div class="container">
      <h1 class="title is-2">{{ heading }}</h1>
      {% if subheading %}<p class="subtitle is-5 mt-3">{{ subheading }}</p>{% endif %}
    </div>
  </div>
</section>
